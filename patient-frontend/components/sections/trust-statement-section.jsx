'use client';

import { TextReveal } from '@/components/ui/text-reveal';

export function TrustStatementSection() {
  return (
    <section className="border-b border-border/60">
      <TextReveal className="h-[110vh]">
        We treat every visit like it matters — because to you, it does. Real dentists, real availability, zero pressure.
      </TextReveal>
    </section>
  );
}
